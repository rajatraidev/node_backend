<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>


    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">


    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.1/jquery.min.js" ></script>
</head>
<body>
    <div>
        <p>
            <%= data.companyName %>
            <%= data.positions %>
            <%= data.city %>
            <%= data.payScale %>
        </p>
    </div>  


    <div>
        <h4>Student Allocated to this interview</h4>
        <table class="table table-bordered table-dark">
        <thead>
            <tr>
                <th>Name</th>
                <th>College</th>
                <th>DSA Final Score</th>
                <th>WebD Final Score</th>
                <th>React Final Score</th>
                <th>Allocate Result</th>
            </tr>
        </thead>
        <tbody>
        <% let i = 1 %> 
        <% for(student of studentList){ %>
           
                <tr>
                    <td><%= student.name %></td>
                    <td><%= student.college %></td>
                    <td><%= student.dsa %></td>
                    <td><%= student.webd %></td>
                    <td><%= student.react %></td>
                    <td>
                        <form action="" method="post" name="resultForm" id="<%= i %>">
                            <select name="result" id="">
                                <option value="pass">Pass</option>
                                <option value="fail">Fail</option>
                                <option value="on hold">On Hold</option>
                                <option value="not attempted">Didn't Attempted</option>
                            </select>
                            <input type="hidden" name="studentId" value="<%= student.id %>">
                            <input type="hidden" name="interviewId" value="<%= data.id %>">
                            <input type="submit" name="submit">
                        </form>
                    </td>

                </tr>
           
        
        <% i++ } %>
    </tbody>
    </table>
    </div>

    <script src="/js/interview-js.js" type="application/javascript"></script>
</body>
</html>